# ==============================================
# Google Cloud - AlloyDB Database
# ==============================================
# Connection string format for AlloyDB:
# postgresql+asyncpg://USER:PASSWORD@PRIVATE_IP:5432/DATABASE
#
# For Cloud SQL Proxy:
# postgresql+asyncpg://USER:PASSWORD@localhost:5432/DATABASE
#
# Example:
ALLOYDB_CONNECTION_URI=postgresql+asyncpg://your-user:your-password@10.0.0.5:5432/trashmapr

# ==============================================
# Google Cloud - Authentication
# ==============================================
# Google OAuth Configuration
GOOGLE_OAUTH_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com
GOOGLE_OAUTH_CLIENT_SECRET=your-google-oauth-client-secret

# Google Application Credentials (path to service account JSON)
# Using gcp_creds.json from project root
# For Docker: file is mounted at /tmp/gcp_creds.json
# For local: use relative path ../gcp_creds.json
GOOGLE_APPLICATION_CREDENTIALS=../gcp_creds.json

# ==============================================
# Google Cloud - AI & Storage
# ==============================================
# Gemini API Configuration
GEMINI_API_KEY=your-gemini-api-key

# Google Cloud Storage Bucket
GCS_BUCKET_NAME=your-gcs-bucket-name

# ==============================================
# Application Configuration
# ==============================================
# Secret key for JWT signing and encryption
# Generate with: openssl rand -hex 32
SECRET_KEY=your-secret-key-for-jwt-signing-change-this-in-production

# OAuth Redirect URI (update with your actual domain)
OAUTH_REDIRECT_URI=http://localhost:8000/auth/callback

# API Port (for docker-compose port mapping)
API_PORT=8000

# Internal server port (Cloud Run uses PORT env var)
PORT=8080

# ==============================================
# Application Settings (Optional)
# ==============================================
# APP_NAME=TrashMapr API
# APP_VERSION=1.0.0
# DEBUG=false

# CORS allowed origins (comma-separated)
# CORS_ORIGINS=*
# For production: CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Database connection pool settings
# DB_POOL_SIZE=10
# DB_MAX_OVERFLOW=20
# DB_ECHO=false

# ==============================================
# Google Cloud Project (Optional)
# ==============================================
# GCP_PROJECT_ID=your-gcp-project-id
# GCP_REGION=us-central1
